import cadquery as cq

# Generated by CadQuarry v0.6.0 — seed 1234004040 — CC0-1.0
# https://github.com/jacobjennings/CadQuarry

PARAMS = {
    "loft_base": {"type": 'float', "default": 50, "min": 25.0, "max": 80.0, "step": 1.0, "group": 'Body', "label": 'Base size (mm)'},
    "loft_top": {"type": 'float', "default": 19.5, "min": 7.8, "max": 60.0, "step": 1.0, "group": 'Body', "label": 'Top size (mm)'},
    "loft_h": {"type": 'float', "default": 55.4, "min": 22.2, "max": 110.8, "step": 1.0, "group": 'Body', "label": 'Height (mm)'},
}


def build(p):

    result = (
        cq.Workplane('XY')
        .polygon(6, p["loft_base"] * 0.5 * 2)
        .workplane(offset=p["loft_h"])
        .rect(p["loft_top"], p["loft_top"])
        .loft(combine=True)
    )
    return result


result = build({k: v["default"] for k, v in PARAMS.items()})
